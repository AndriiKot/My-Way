
> >> 

sort names.txt > names_sorted.txt
  > - перенаправления вывода
    если файл 'names_sorted.txt' не существует  
    то он создаться или пересоздаться с новым 
    содержимым тоесть перезапишеться 
   

sort -n numbers.txt >> names_sorted.txt
    >> - добавит в конец файла 'names_sorted.txt'
        отсортированное содержимое файла 'numbers.txt'
        если файл с имене 'names_sorted.txt' не существует
        то он создаться

   !!! DANGER !!!
        sort names.txt > names.txt
      сначала создаётся файл names.txt 
      а так как он создаётся всегда пустым то
      и сортировать там нечего 

!
sort -n numbers.txt >> numbers.txt
    Будет довавленна в конец файла содержимое
    этого же файла только отсортированое

grep andrii /etc/* 2> /dev/null
    вывод всех ответов в консоль которые завершились с ошибкой





   


