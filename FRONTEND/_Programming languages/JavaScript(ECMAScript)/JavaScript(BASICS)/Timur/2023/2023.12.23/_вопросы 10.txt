
10. 10-1
    Создайте объект 'auto', создайте у этого объекта поле 'brand' со значение "Tesla"
    Создайте для этого объекта метод 'dirve' содержимое которого будет возвращать строку
    `Завёдем нашу ${this.brand}`  
  // 10-2
  * Вызовете у объекта 'auto' метод 'drive'
  // 10-3
  * Создайте новую переменную которая бы наследовала метод 'drive' из 'auto' 
    и при вызове  этой переменной this бы ссылалась на 'auto'
  // 10-4
  * Создайте новый объекты 'motorBike' , создайте у этого объекта поле 'brand' со значением "Suzuki"
    Создайте переменную 'motorDriveBike' которая ссылалась бы на объект 'motorBike' у которого можно было
    вызывать метод 'drive'
  // 10-5 
  * Создайте переменную 'motorDrivePlan' у которого поле 'brand': 'Midway' и вызовете её так чтобы у объект 
   отвечал на поле 'drive'
  // 10-6
  *

  // 10-7
  * const payCount = bill.calculate.bind(bill).bind(({ tip: 0.2 }).bind({ tip: 0.5 });
    console.log(payCount(1000));

  // 10-8
  

   

    

    