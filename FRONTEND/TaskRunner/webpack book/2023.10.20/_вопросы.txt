
1. Через командную строку создайте папку проекта.

2. Через командную строку перейдите в эту папку.

3. Инициализируйте свой проект при помощи npm.

4. Установите пакет webpack и webpack-cli.

5. В какую папку будет установлен webpack?

6. Подключите библиотеку lodash через скрипт который будет находиться в теге head.

7. Создайте файл js. В котором функция будет
*создайте html элемент
*затем добавьте в этот элемент строку
*строку образуйте при помощи преобразования массива ['Hello','webpack'] межу элементами массива должен идти один пробел.
*функции должна вернуть новый html элемент
*добавьте результат вычисление этой функции в конец узла html документа body

8. Что нужно сделать для предотвращения случайной публикации кода?

9. Как подключаются пакеты через npm которые нужны для продушена и как подключаются те которые
только участвуют в разработке?

10. Подключите библиотеку lodash через npm и удалите подключение через скрипт в теге head.
Подключите библиотеку к самому скрипту где используется lodash.

11. Запустите  webpack, который примет  скрипт в src/index.js качестве точки входа и сгенерирует
его dist/main.js в качестве вывода.