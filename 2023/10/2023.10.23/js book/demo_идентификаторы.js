"use sctrict";

// Переменная
let amount = 100;
let price = 200;

console.dir({amount});

amount = 0;

console.dir({amount});

amount += price;

console.dir({amount});


// Глобальная константа
const MAX_VALUE = 2000;

// Локальная константа
const total = 1550;

// Good practice
let year = 622;
let migration = 'Hijrah';
let i;
let counter = 0;

// Промежуточные перемнные

// const goods = order[groupName];
// const amount = calculateSubtotal(goods);
// expensee.set(groupName, amount);




