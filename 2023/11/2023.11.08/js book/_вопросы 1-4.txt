1. Дан объект 'values' как можно пробежаться по полям этого объекта?
Примените к каждому значению поля операцию 'typeof' и выедете результат с
новой строки для каждого значения поля.

const values = {
  name: 'Mouse',
  price: 25,
  stock: false,
  avg: (a, b) => (a + b) / 2n,
  reference: null,
  primitive: undefined,
  numbers: [NaN, Infinity],
}

2. Как можно изменить время выполннения куска кода JS?

const fn1 = (a = 0,b = 0) => (a + b);
  const fn2 = (a,b) => {
  a = a || 0;
  b = b || 0;
  return a + b
};


3. Как можно итерировать массив?
  Посчитайте сумму всех елементов массива.
  const myArray = [11, 22, 33, 44];


4. Создайте асинхронную функцию, вызовете её и присвойте значение в переменную и напечатайте результат в консоль.
const fn3 = () => {
    console.log(`Hello I am function:\n ${fn1}!`)
};


console.log(fn3());
