
1.
.gitignore

2. Создаём файл '.gitignore' и заносим туда каталог 'node_modules' так как там хранятся сторонние
пакеты

3.
npm install

or

npm i

4.
npm install --production


5. Это значить что у данного пакета нет зависимостей от других пакетов.

6. Это значит что у данного пакета есть одно Dev зависимость которая на продакшен не идёт.

7. В кладек Dependents 
   Например: 787 Dependents

8. 
  npm i is-url
