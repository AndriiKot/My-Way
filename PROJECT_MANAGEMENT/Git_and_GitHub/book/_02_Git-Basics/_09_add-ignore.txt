
git commit -a
  -a индексирует все изменения 
     которые не попали в индекс
        то-есть с этим флагом можно 
        не писать перед коммитов git add .

Example:
git commit -a -m "Ignore index"
