

1. Через командную строку создайте папку проекта.

2. Чеезе командную строку перейдите в эту папку.

3. Инициализируйте свой проект при помощи npm.

4. Устоновите пакет webpack и webpack-cli.

5. В какую папку будет установлен webpack?

6. Подключите библиотеку lodash через скрипт конотрый будет находится в теге head.

7. Создайте вайл js. В котором функция будет 
    *создовать html елемент 
    *затем довабете в этот эелемент строку
    *строку образуйте при помоши преоброзования массива ['Hello','webpack'] межу елементами массива должен ити один пробел.
    *функции должна вернуть новый html елемент
    *добавте результат вычесление этой функции в конец узла html документа body

8. Что нужно сделать для предотвращения случайной публикации кода?

9. Как подключаются пакеты через npm которые нужны для продакшена и как подлючаются те которые 
   только участвуют в разработке?

10. Потключите библиотеку lodash через npm и удалите подключение через скрипт в теге head.
    Подключите библиотеку к самому скрипту где используется lodash.

11. Запустите  webpack, который примет  скрипт в src/index.js качестве точки входа и сгенерирует 
    его dist/main.js в качестве вывода.



















   