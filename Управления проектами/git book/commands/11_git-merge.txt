
git merge <name branch>
    Example:
        Если мы хотим в сетку 'main' слить её с веткой 'fixing'
        то переходим на ветку 'main'
        git checkout main
        git branch  
            main
        git merge fixing
