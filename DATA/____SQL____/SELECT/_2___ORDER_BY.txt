
SELECT title, film_id, rating
FROM film f
ORDER BY length;

-- Сортировка по невыбранным полям
-- Данные могут сортироватся и по столбцам, которые не
-- извлекаются в самом запросу

SELECT title, film_id, rating
FROM film f
ORDER BY title, length;

-- Сортировка по нескольким столбцам


-- СОРТИРОВКА ПО ПОЛОЖЕНИЮ СТОЛБЦА
SELECT title, film_id, rating
FROM film f
ORDER BY 2, 3;

-- Недостатки такого метода в том что можно случайно
-- изменить порядок столбцов при исзменении списка SELECT
-- НЕЛЬЗЯ использовать этот метод для сортировки по столбцам, не
-- указанным в списке SELECT.








