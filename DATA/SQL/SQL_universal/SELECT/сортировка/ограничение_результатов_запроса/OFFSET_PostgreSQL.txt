SELECT title
FROM film
OFFSET 10;

!!! Число строк начинается с 0 
    поэтому OFFSET 1 вернёт вторую строку а не первую
!!!

!!! Порядок не определён. Вывод будет все строки кроме первых десяти.
    (Порядок случайный)
!!!

